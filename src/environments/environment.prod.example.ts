// Vorlage: Diese Datei nach environment.prod.ts kopieren und einen eigenen,
// zufälligen Token eintragen. environment.prod.ts ist in .gitignore und wird
// niemals committet.
export const environment = {
  production: true,
  accessToken: 'REPLACE_WITH_REAL_TOKEN',
};
