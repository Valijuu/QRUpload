export const environment = {
  production: false,
  // Nur für die lokale Entwicklung (ng serve). Für den Produktions-Build wird
  // stattdessen environment.prod.ts verwendet, siehe README.
  accessToken: 'LOCAL_DEV_TOKEN',
  uploadUrl: '',
  weddingImageUrl: '',
};
